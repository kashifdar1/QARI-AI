/**
 * Reserved shell for the reviewer/admin web portal (CLAUDE.md §1: "later").
 * No features in Milestone 0 — this only proves the app builds and typechecks.
 */
export function App(): JSX.Element {
  return <div>Qari AI Admin — post-MVP</div>;
}
