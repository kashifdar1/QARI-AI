# Expo only exposes vars prefixed EXPO_PUBLIC_ to client code — nothing
# secret belongs in this file or its prefix.
# Copy to .env.development / .env.staging / .env.production.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000/v1
EXPO_PUBLIC_APP_ENV=development
