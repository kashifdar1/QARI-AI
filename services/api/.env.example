# Copy to .env.development / .env.staging / .env.production as appropriate.
# Loaded and validated at startup via @qari/config (parseApiEnv) — the
# process refuses to start if any of these are missing or malformed.
NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://qari:qari@localhost:5432/qari
REDIS_URL=redis://localhost:6379
OBJECT_STORAGE_ENDPOINT=http://localhost:9000
# Optional — only needed when the server and the client hitting presigned
# URLs (e.g. an Android emulator) can't reach the bucket at the same host.
# Android emulator: http://10.0.2.2:9000. Physical device on the same LAN:
# the host machine's LAN IP. Falls back to OBJECT_STORAGE_ENDPOINT if unset.
# OBJECT_STORAGE_PUBLIC_ENDPOINT=http://10.0.2.2:9000
OBJECT_STORAGE_BUCKET=qari-audio-dev
OBJECT_STORAGE_ACCESS_KEY_ID=qari-dev
OBJECT_STORAGE_SECRET_ACCESS_KEY=qari-dev-secret
SIGNED_URL_TTL_SECONDS=300
JWT_SECRET=dev-only-change-me
